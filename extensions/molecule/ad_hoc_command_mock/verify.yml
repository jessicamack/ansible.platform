---
- name: Verify — ad hoc commands were created on mock server
  hosts: localhost
  connection: local
  gather_facts: false
  vars:
    gateway_hostname: "http://127.0.0.1:8000"
    mock_auth: "Basic bW9jazptb2Nr"
  tasks:
    - name: List ad hoc commands from mock Controller API
      ansible.builtin.uri:
        url: "{{ gateway_hostname }}/api/controller/v2/ad_hoc_commands/"
        method: GET
        headers:
          Authorization: "{{ mock_auth }}"
        return_content: true
        status_code: 200
      register: commands_check
      vars:
        ansible_connection: local

    - name: Assert at least 10 commands were created (6 via persistent/local, 4 via direct mode; check_mode launches none)
      ansible.builtin.assert:
        that:
          - commands_check.json.count >= 10
        fail_msg: >-
          Verify: expected at least 10 ad hoc commands.
          Got: {{ commands_check.json.count }}
      vars:
        ansible_connection: local
...
